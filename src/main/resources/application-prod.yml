spring:
  data:
    mongodb:
      uri: ${MONGODB_URI}
      database: journaldb
      auto-index-creation: true
  main:
    allow-circular-references: true
server:
  port: ${SERVER_PORT}
  servlet:
    context-path: /journal

